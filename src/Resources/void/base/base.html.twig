{#
  This file is auto-generated.

  Base - The base element specifies the base URL to use for all relative URLs in a document. There can be at maximum one <base> element in a document, and it must be inside the <head> element.

  Symfony UX Twig Component (Anonymous)
  @see https://symfony.com/bundles/ux-twig-component/current/index.html

  Usage:
    <twig:Base target="_blank" />

  @author vardumper <info@erikpoehler.com>
  @package vardumper/extended-htmldocument
  @see src/TemplateGenerator/TwigComponentsGenerator.php
#}
{% apply spaceless %}
{%- set _attrs = {} -%}
  {% if this.href is defined and this.href is not null and this.href != '' %}
    {% set _attrs = _attrs|merge({('href'): this.href}) %}
  {% endif %}
  {% if this.target is not null and this.target.value != '' %}
    {% set _attrs = _attrs|merge({('target'): this.target.value}) %}
  {% endif %}<base{%- for __k, __v in _attrs -%}{% if __v is not null and __v != '' %}{{ (' ' ~ __k ~ '="' ~ __v|e('html_attr') ~ '"')|raw }}{% endif %}{%- endfor %}
/>
{% endapply %}
